name: Testing during CI

on:
  pull_request:
    types: [opened, synchronize]
    branches: main

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm run lint
